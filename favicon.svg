<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" fill="none">
  <!-- Background: parchment rounded square -->
  <rect width="64" height="64" rx="14" fill="#f5f4ed"/>
  <rect width="64" height="64" rx="14" stroke="#e8e6dc" stroke-width="1"/>

  <!-- Header bar (short) -->
  <rect x="21" y="17" width="22" height="7" rx="3.5" fill="#c96442"/>

  <!-- Dot · -->
  <circle cx="32" cy="28" r="2" fill="#c96442" opacity="0.4"/>

  <!-- Payload bar (longest) -->
  <rect x="13" y="32" width="38" height="7" rx="3.5" fill="#c96442" opacity="0.7"/>

  <!-- Dot · -->
  <circle cx="32" cy="43" r="2" fill="#c96442" opacity="0.4"/>

  <!-- Signature bar (medium) -->
  <rect x="17" y="47" width="30" height="7" rx="3.5" fill="#c96442" opacity="0.4"/>
</svg>
